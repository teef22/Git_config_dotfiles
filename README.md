Git config repo. Put to `~/.config/git/`.

After cloning the repo create `private_config.gitconfig` and `private_university.gitconfig` to store attributes with personal info (like `name = ...` and `email = ...`).
